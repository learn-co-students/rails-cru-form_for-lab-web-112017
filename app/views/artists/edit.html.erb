<h1>CEdit Artist Form</h1>

<%= form_for(@artist) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: 'Name' %>
  <%= f.label :bio %>
  <%= f.text_field :bio, placeholder: 'Bio' %>
  <%= f.submit %>
<% end %>
